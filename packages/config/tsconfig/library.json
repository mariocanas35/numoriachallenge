{
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "Numoria — Library (packages internos consumidos via transpilePackages)",
  "extends": "./base.json",
  "compilerOptions": {
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "noEmit": true,
    "jsx": "preserve"
  }
}
