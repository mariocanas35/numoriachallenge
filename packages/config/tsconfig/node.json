{
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "Numoria — Node / scripts / edge functions",
  "extends": "./base.json",
  "compilerOptions": {
    "lib": ["ES2022"],
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "noEmit": true,
    "types": ["node"]
  }
}
