/**
 * Tipos públicos de la base de datos.
 *
 * Re-exporta los tipos generados desde el schema Supabase.
 * Para regenerar: `pnpm db:types`
 */

export type { Database, Tables, TablesInsert, TablesUpdate, Enums } from './types.gen';
